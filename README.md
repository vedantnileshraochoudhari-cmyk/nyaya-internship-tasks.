Embeddings Cluster — Day 01

This project is part of an internship task.
It generates 100 sentences, creates mock embeddings, clusters them using KMeans (k=3),
visualizes the clusters using PCA, and saves the outputs.
